import{mount}from'@vue/test-utils';import{describe,expect,it}from'vitest';import VerificationChecklist from './VerificationChecklist.vue';
describe('VerificationChecklist',()=>{it('renders checklist and residual issues',()=>{const wrapper=mount(VerificationChecklist,{props:{checklist:{decision:'Minor Revision',items:[{id:'R1',originalComment:'Add evidence',authorClaim:'Added',status:'FULLY_ADDRESSED',verified:true,qualityAssessment:'Sound'}],residualIssues:['Clarify scope'],newIssues:[]}}});expect(wrapper.text()).toContain('FULLY_ADDRESSED');expect(wrapper.text()).toContain('Clarify scope');});});
